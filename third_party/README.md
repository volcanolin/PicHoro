# Vendored plugins

Patched copies of third-party Flutter plugins required to build this fork
with modern Flutter / Android Gradle Plugin. See `/BUILD.md`.

Do not replace with unpatched pub.dev versions without re-testing the Android release build.
